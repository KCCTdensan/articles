{
  "name": "articles",
  "version": "2.0.0",
  "dependencies": {
    "front-matter": "^4.0.2",
    "marked": "^4.0.18",
    "tiny-glob": "^0.2.9"
  },
  "private": true,
  "scripts": {
    "build": "mkdir -p ./dist/api && node ./build.js"
  }
}
