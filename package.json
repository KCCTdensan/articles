{
  "name": "articles",
  "version": "2.0.0",
  "dependencies": {
    "front-matter": "4.0.2",
    "marked": "4.2.5",
    "tiny-glob": "0.2.9"
  },
  "private": true,
  "scripts": {
    "build": "node build",
    "prebuild": "rm -rf dist && mkdir -p dist/api"
  }
}
